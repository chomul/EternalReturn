# ✅ F09 — 제작 · 에디터 작업 및 테스트

> Task [`../1_Task/F09_제작/00_개요.md`](../1_Task/F09_제작/00_개요.md)
> 근거 [`../4_Argument/20_아이템데이터_저장방식.md`](../4_Argument/20_아이템데이터_저장방식.md) (결과 행에 재료 2개)

# F09-01 — 조합 데이터 · 재귀 트리 탐색

> ⚠ `FERItemRow` 에 UPROPERTY 2개 추가 (`CraftMaterialA / B`) — **에디터 재시작**. 기존 `DT_Items` 행은 그대로 (끝에 붙였다)

## 구조

```
FERItemRow.CraftMaterialA / B         결과 행에 재료 2개 (둘 다 비면 기본 재료)
ERCraft (Item/ERCraftLibrary)         순수 함수 — 테이블만 읽는다
  FindResult(A, B)   정방향 (순서 무관)      FindUsing(M)   역방향       ExpandLeaves(C)   잎 재료 + 수량 (순환 → Error)
  인덱스 1회 생성 (Forward · UsedIn) — 재료 ID 오타 · 한쪽만 채움 · 같은 조합 두 결과 → Error 로그
```

## 애셋 — `DT_Items` 에 망치 계열 17종 + 재료 (역기획서 §3 표)

⭐ **손으로 안 친다.** `DT_Items` 우클릭 → **Export as CSV** → `Docs/3_EditorTasks/Data/DT_Items_export.csv` 로 저장해서 알려주면, 그 형식(`StatModifiers` 맵 문법)에 맞춰 아래 행을 붙인 **Reimport 용 CSV** 를 만든다.

행 이름(ID)은 이걸로 고정한다 (재료 참조 · 나중 저장 인벤토리가 이 이름을 쓴다):

| ID | 이름 | 등급 | 재료 A + B |
|---|---|---|---|
| `hammer` | 망치 | Common · Weapon · Hammer | (기본) |
| `deer_hammer` | 사슴 망치 | Uncommon | `hammer` + `rubber` |
| `warhammer` | 워해머 | Uncommon | `hammer` + `nail` |
| `morning_star` | 모닝스타 | Rare | `warhammer` + `iron_ball` |
| `fate_hammer` | 운명의 망치 | Rare | `deer_hammer` + `playing_cards` |
| `wolf_fang_club` | 낭아봉 | Hero | `morning_star` + `horse_tally` |
| `dagda_hammer` | 다그다의 망치 | Hero | `warhammer` + `diamond` |
| `knowledge_is_power` | 아는 것이 힘 | Hero | `deer_hammer` + `hardcover` |
| `thousand_weight` | 천근추 | Hero | `fate_hammer` + `glass_bottle` |
| `thor_hammer` | 토르의 망치 | Hero | `fate_hammer` + `battery` |
| `evening_star` | 개밥바라기 | Legend | `hammer` + `meteorite` |
| `doomsday` | 둠스데이 | Legend | `iron_ball` + `mithril` |
| `leviathan` | 레비아탄 | Legend | `hammer` + `mithril` |
| `squeaky_hammer` | 뿅망치 | Legend | `hammer` + `tree_of_life` |
| `peacebringer` | 피스브링어 | Transcendent | `hammer` + `vf_blood_sample` |
| `peacebringer_crimson` | 피스브링어-진홍 | Transcendent | `peacebringer` + `crimson_shard` |
| `peacebringer_dawn` | 피스브링어-새벽 | Transcendent | `peacebringer` + `dawn_shard` |

재료 (Slot None · 재료는 기본): `rubber` 고무 · `nail` 못 · `iron_ball` 쇠구슬 · `playing_cards` 트럼프 카드 · `horse_tally` 마패 · `diamond` 다이아몬드 · `hardcover` 하드커버 · `glass_bottle` 유리병 · `battery` 배터리 · `meteorite` 운석 · `mithril` 미스릴 · `tree_of_life` 생명의 나무 · `vf_blood_sample` VF 혈액 샘플 · `crimson_shard` 진홍의 샤드 · `dawn_shard` 새벽빛 샤드
공격력은 §3 표 값을 `StatModifiers.AttackPower` 에 (기존 `hammer_t1` 등 테스트 행 7개는 그대로). 재료 등급 · 스택 수는 `[자체]` (보스 재료 Legend/Transcendent · 스택 1).

순환 테스트용 행 하나: `cycle_a` | 재료 `cycle_b` + `rubber` · `cycle_b` | 재료 `cycle_a` + `rubber` (검증 끝나면 지운다)

- [x] `DT_Items` Export as CSV → `Data/DT_Items_export.csv` (UTF-16) — 2026-09-18
- [x] ⭐ [`Data/DT_Items.csv`](Data/DT_Items.csv) (기존 7행 그대로 + 34행) 를 콘텐츠 브라우저 **`Content/Item/`** 에 드래그 → Row Type `ERItemRow` → 이름이 `DT_Items` 로 같으니 **"이미 있음 → 덮어쓰기(Replace)"** 확인. 기존 참조(설정 · 인벤토리 ID)는 같은 경로라 그대로. 열어서 41행 확인
- [x] 11:01:51 `[제작] 인덱스 생성 — 행 41 · 조합 18` (망치 16 + cycle 2 — 순환 행도 조합으로 세어진다)

## 검증 — PIE 1인 (네트워크 없음)

### ① 트리 · 잎
```
ER.Craft.Tree thor_hammer     → thor_hammer(토르의 망치)
                                  fate_hammer(운명의 망치)
                                    deer_hammer(사슴 망치)
                                      hammer(망치)  <- 잎
                                      rubber(고무)  <- 잎
                                    playing_cards(트럼프 카드)  <- 잎
                                  battery(배터리)  <- 잎
                                잎 재료: hammer x1 rubber x1 playing_cards x1 battery x1
```
- [x] 10:35:55 트리 정확 · `잎 재료: hammer x1 rubber x1 playing_cards x1 battery x1`

### ② ⭐ 초월 건너뛰기
```
ER.Craft.Tree peacebringer_crimson → 잎 재료: hammer x1 vf_blood_sample x1 crimson_shard x1
```
- [x] 11:01:43 `peacebringer_crimson → peacebringer → hammer · vf_blood_sample / crimson_shard` — 잎 3종

### ③ 역방향 · 정방향
```
ER.Craft.With hammer          → 6개: deer_hammer warhammer evening_star leviathan squeaky_hammer peacebringer
ER.Craft.Result rubber hammer → deer_hammer     (순서 바꿔도 같다)
ER.Craft.Result hammer hammer → (없음)
```
- [x] `With hammer` 6개 · `Result rubber hammer -> deer_hammer` (데이터는 A=hammer B=rubber — 역순 조회 성공) · `hammer hammer -> (없음)`

### ④ 순환 · 오타
```
ER.Craft.Tree cycle_a         → Error [제작] 순환 조합: cycle_a -> cycle_b -> cycle_a. 데이터 오류 — 탐색 중단.  · 잎 계산 실패
```
- [x] 11:02:09 `Error [제작] 순환 조합: cycle_a -> cycle_b -> cycle_a. 데이터 오류 — 탐색 중단.` + `잎 계산 실패` — 크래시 없음 (트리 출력은 디버그 상한 32 까지 찍힌 것, 정상)
- [ ] (선택) 재료 한 칸을 일부러 `rubbr` 로 고치고 PIE → 시작 로그 `Error [제작] … 재료 'rubbr' 가 DT_Items 에 없다 (오타?)` (고친 뒤 원복)

### ⑤ 인덱스 1회
- [x] 세션 전체에서 `[제작] 인덱스 생성` **1회** (`Tree` 는 Find 만 써서 `With` 때 처음 생성) · 에디터에서 행을 고치면 다음 조회 때 다시 한 번

---

# F09-02 — 제작 실행

> 애셋 작업 없음 (설정 `ER Growth > CraftProficiencyExpByGrade` 100/200/350/550/800/800 · `FirstCraftBonus` 0.25 는 기본값). cpp 만 바뀌어 에디터 재시작 불필요
> `DA_TestDummy.WeaponTypes` 에 **Hammer** 가 있어야 한다 (지금 재키 = Dagger · TwoHandSword · Axe · DualSword → **Hammer 추가**). `cycle_a` · `cycle_b` 행은 지운다

## 구조

```
클라 ER.Craft.Make <id> → ServerCraft(id) [WithValidation]
  서버 Craft(): ① 행 · 재료 2개 → ② 재료 출처 계획 (가방 우선 → 장착) → ③ 결과 자리 (장착 재료 소비 + 결과 장비 → 그 슬롯 / 아니면 가방 여유 계산) → ④ 적용 → OnItemCrafted
  ⭐ ①~③ 은 읽기만 — 실패는 아무것도 안 바꾼다
PlayerState: OnItemCrafted → Growth->OnItemCrafted → 결과가 무기면 그 무기군 숙련도 += 등급값 × (최초 1.25)
```

- [ ] `DA_TestDummy.WeaponTypes` 에 Hammer 추가 · `cycle_*` 행 삭제

## 검증 — `Play As Listen Server`, 2명 · **클라 창**에서 (실제 RPC 경로)

### ① ⭐ 기본 제작 — 가방 재료 → 가방
```
(서버 창) ER.Item.Give hammer 1 all → ER.Item.Give rubber 1 all      (⚠ `all` 이 있어야 클라(PS_1)에게도 간다 — 없으면 서버 플레이어만)
(클라 창) ER.Item.Bag                  → hammer, rubber
(클라 창) ER.Craft.Make deer_hammer    → 서버 로그 [제작] ERPlayerState_1: hammer + rubber -> deer_hammer (가방에 · 최초)
                                         [숙련도] ERPlayerState_1 Hammer +250.0 (제작 · 최초) -> 250.0 / 230 (Lv.1) → 레벨업 -> Lv.2 (남은 20.0)
(클라 창) ER.Item.Bag                  → deer_hammer 만 (재료 사라짐)
```
- [x] (서버 플레이어 경로) 11:22:46 `ERPlayerState_0: hammer + rubber -> deer_hammer (가방에 · 최초)` · `Hammer +250.0 (제작 · 최초)` → Lv.2 (남은 20) · 가방에 deer_hammer 만
- [x] **클라 RPC 경로** — 11:41:49 클라 `Make` → `ERPlayerState_1 -> hammer ×1 제거 (칸 0)` · `rubber ×1 제거 (칸 1)` · `<- deer_hammer ×1` · `[제작] ERPlayerState_1: hammer + rubber -> deer_hammer (가방에 · 최초)` · `Hammer +250.0` → Lv.2

### ② ⭐ 재료 부족 → 거부, 변화 없음
```
(클라 창) ER.Craft.Make deer_hammer    → [제작] … 거부 — 재료 hammer 가 없다 (deer_hammer 를 만들려면 hammer + rubber).
(클라 창) ER.Item.Bag                  → deer_hammer 그대로
```
- [x] 11:26:59 `거부 — 재료 hammer 가 없다` · 가방 `deer_hammer×1` 그대로

### ③ ⭐ 장착 중 재료 → 결과가 장착 슬롯에 `[자체]`
```
(서버 창) ER.Item.Give hammer 1 all → ER.Item.Give rubber 1 all
(클라 창) ER.Item.Bag → hammer 가 [k]칸 → ER.Item.EquipSlot k     (클라는 EquipSlot <가방칸>. `Equip` 은 서버 전용)
                                       → 장착 · [숙련도] Hammer Lv.2 -> … 적용 (Amp 없으면 Warning — DA 에 Hammer 계수 없음, 정상)
(클라 창) ER.Item.Stats                → AttackPower 추가 +16
(클라 창) ER.Craft.Make deer_hammer    → [제작] … 장착 중이던 hammer 를 재료로 소비 → [장비] … <- deer_hammer 장착 → [제작] … -> deer_hammer (장착 슬롯에)   ← "최초" 없음
                                         [숙련도] Hammer +200.0 (제작) …   ← 두 번째라 ×1.25 없음
(클라 창) ER.Item.Stats                → AttackPower 추가 +25 (망치 16 → 사슴 망치 25)
(클라 창) ER.Item.Equipped             → Weapon: deer_hammer
```
- [x] 11:49:31 `장착 중이던 hammer 를 재료로 소비` → `[장비] <- deer_hammer 장착` → `(장착 슬롯에 · 최초)` · `Stats` 추가 +16 → **+25** · (새 PIE 라 최초 250 — 두 번째 200 은 ① 세션에서 구조 동일)

### ④ 없는 조합 · 없는 ID → 거부
```
(클라 창) ER.Craft.Make hammer         → 거부 — hammer 는 제작할 수 없다 (재료 없음)
(클라 창) ER.Craft.Make nothing_x      → Error [아이템] 'nothing_x' 행이 DT_Items 에 없다 · 거부
```
- [x] 11:42:05 `hammer 는 제작할 수 없다 (재료 없음)` · 11:42:09 `Error 'nothing_x' 행이 DT_Items 에 없다` + 거부

### ⑤ 같은 재료 2개 · 자리 없음 (구조만)
- [x] (구조만) 같은 재료 2개 조합 — 계획 단계가 같은 칸에서 2개를 센다
- [x] (구조만) 가방 10/10 + 장착 재료로 결과가 가방행일 때 거부

---

# F09-03 — 재료 획득 — 채집 · 상자

> ⚠ 새 USTRUCT (`FERLootRow` · `FERLootEntry`) · `UERItemSettings.LootTable` · `AERItemDropActor.LootRow` — **에디터 재시작**
> ⚠ `Config/DefaultGame.ini` 에 `[/Script/EternalReturn.ERItemSettings] LootTable=…` 를 에디터가 쓴다

## 구조

```
AERItemDropActor + LootRow(FName)        ← 상자 · 채집물 = "미리 채워진 시체". 새 클래스 · 새 RPC 없음
  BeginPlay(서버) / InitializeFromLoot   → ERLoot::Find(행) → Roll (가중치 · RollCount 종 중복 없이 · Weight 0 제외) → Items 복제
  상자: TakeFromSlot 로 비면 Destroy — 한 번 가져가면 끝 (사용자 확인)
  채집물(bInfinite): 줄지 않는다 · ServerPickup → TryBeginGather 점유 → GatherSeconds 뒤 FinishGather 가 지급 · 그 사이 다른 사람은 거절 (한 번에 한 명, 사용자 확인)
습득 = ServerPickup (F08-05 그대로 — 거리 · 경쟁 판정)
```

## 애셋 2개 + 1

### 1. `DT_Items` 갱신 — [`Data/DT_Items.csv`](Data/DT_Items.csv) 다시 임포트 (덮어쓰기)
`cycle_a/b` 삭제 · `branch` 나뭇가지 · `flower` 꽃 추가 (41행)
- [x] 덮어쓰기 임포트 — 2026-09-18 (`branch` 롤 성공)

### 2. `DT_Loot` — [`Data/DT_Loot.csv`](Data/DT_Loot.csv) 를 `Content/Item/` 에 드래그 → Row Type **`ERLootRow`** → 이름 `DT_Loot`

| 행 | 항목 (가중치) | RollCount | bInfinite | GatherSeconds |
|---|---|---|---|---|
| `Box_Basic` | rubber 3 · nail 3 · iron_ball 2 · playing_cards 2 · glass_bottle 2 · **stone 0** | 2 | ✘ | — |
| `Box_Uncommon` | battery 2 · hardcover 2 · diamond 1 · horse_tally 1 | 1 | ✘ | — |
| `Gather_Branch` / `Gather_Stone` / `Gather_Flower` | 1종 ×1 확정 | 1 | **✔** | **2** `[자체]` |
| `Bad_Row` | rubber · **no_such_item** (오타 검증용, 끝나면 삭제) | 1 | ✘ | — |

상자 확률 · 채집 시간은 `[자체]`. 상자 1회성 · 채집 무한/한 명은 사용자 확인 (2026-09-19).
- [x] `DT_Loot` 생성 · 6행

### 3. Project Settings > Game > ER Items > **`Loot Table` = `DT_Loot`**
- [x] 지정

## 검증 — `Play As Listen Server`, 2명

### ① 상자 스폰 · 내용 복제 — 서버 창
```
ER.Loot.Spawn Box_Basic       → [루트] #1 Box_Basic 채움: rubber×2 nail×1   (2종 — 매번 다름)
ER.Item.Drops                 → #1 [0]rubber×2 [1]nail×1
(클라 창) ER.Item.Drops       → 같은 내용
```
- [x] 15:21:02 `#1 Box_Basic 채움: nail×2 glass_bottle×1` (2종) · 클라 `Drops` 동일
- [ ] (선택) Weight 0 `stone` 미출현 — 2회 스폰(nail·glass_bottle / glass_bottle·playing_cards) 에서 없음

### ② ⭐ 습득 = F08-05 경로 — 클라 창 (서버 플레이어 앞에 스폰되니 클라 폰을 옆으로 이동)
```
ER.Item.Pickup 1 0            → [습득] ERPlayerState_1 <- #1 칸 0: rubber ×2 · 클라 Bag 에 rubber×2
```
- [x] 거리 밖 거절 — 15:21:20 `ERPlayerState_1 — 시체 #1 까지 405cm > 200cm`
- [x] 안이면 성공 — 15:31:01 클라 `Pickup 1 0` → `ERPlayerState_1 <- 시체 #1 칸 0: glass_bottle ×1`
- [x] ⭐ 경쟁 — F08-05 ④ 에서 검증된 경로 그대로 (상자는 같은 액터 · 같은 RPC)

### ③ ⭐ 채집물 — 무한 · 한 번에 한 명 (두 폰을 나란히 세우고)
```
(서버 창) ER.Loot.Spawn Gather_Branch → [루트] #2 Gather_Branch 채움: branch×1
(서버 창) ER.Item.Pickup 2 0          → [습득] ERPlayerState_0 — #2 채집 시작 (2.0초)
(클라 창, 바로) ER.Item.Pickup 2 0    → [습득] ERPlayerState_1 — #2 는 ERPlayerState_0 가 캐는 중이다.   ← 거절
(2초 뒤 서버 로그)                    → [습득] ERPlayerState_0 <- 채집물 #2: branch ×1
(클라 창, 다시) ER.Item.Pickup 2 0    → 채집 시작 → 2초 뒤 ERPlayerState_1 <- 채집물 #2: branch ×1
ER.Item.Drops                         → #2 [0]branch×1 그대로 (줄지 않음) · 몇 번을 캐도 그대로
```
- [x] 서버 `Pickup 2 0` → 15:21:31 `채집 시작 (2.0초)` → 15:21:33 `<- 채집물 #2: branch ×1` · `Drops` 에 `#2 [0]branch×1` 그대로 (무한 · 안 사라짐)
- [x] ⭐ 15:31:17 서버 `채집 시작 (2.0초)` → 15:31:19 클라 `#2 는 ERPlayerState_0 가 캐는 중이다.` **거절** → 15:31:19.9 서버 지급 → 15:31:23 클라 `채집 시작` → 25 지급. 총 4회 캐도 `branch×1` 그대로
- [ ] (선택) 상자 두 칸 다 가져가면 `#N 가 비어 사라진다` — F08-05 ⑤ 와 같은 경로

### ④ 오타 · 없는 행
```
ER.Loot.Spawn Bad_Row         → Error [아이템] 'no_such_item' 행이 DT_Items 에 없다 · 채움: rubber×1 (그 항목만 빠진다)
ER.Loot.Spawn Nope            → Error [루트] 'Nope' 행이 LootTable 에 없다 · 채움: (없음)
```
- [x] 15:22:06 `'no_such_item' 행이 DT_Items 에 없다` ×2 (Find + Roll) · `#3 Bad_Row 채움: rubber×1` · 15:22:11 `'Nope' 행이 LootTable 에 없다` · `채움: (없음)`

### ⑤ 맵 배치 (선택)
- [ ] `AERItemDropActor` 를 맵에 놓고 `LootRow = Gather_Stone` → PIE 시작 시 서버가 채운다 (`[루트] #N Gather_Stone 채움`) · 클라에 보인다

---

# ⚠ 아직 못 하는 것

| 항목 | 언제 |
|---|---|
| 연쇄 제작(잎 재료만으로 여러 단계) · 제작 가능 알림 | F17 UI 가 `ExpandLeaves` 보고 `ServerCraft` 반복 |
| 장착 재료 소비 시 자동 장착 규칙 원작 확인 | (미확인) `[자체]` — 확인되면 `Craft()` ③ 한 곳 |
| 상자 · 채집물 메시 · 상호작용 키 · 지역별 배치 · **채집 중 이동 취소 · 진행 표시** | F13 (배치) · F17 (UI · 메시 · 상호작용) |
| 원격 전송 드론 (10 크레딧) | F15 |
| 파밍 위치 오버레이 (행 ↔ 지역 역산) | F17 |
| 제작 UI · 파밍 위치 오버레이 | F17 |
