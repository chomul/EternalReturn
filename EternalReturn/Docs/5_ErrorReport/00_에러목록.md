# 에러 기록

**코드 개발 중 발생한 문제**를 여기에 쌓는다. 원인을 찾은 것만 적는다 — 추측은 적지 않는다.

## 기록 대상

| 적는다 | 적지 않는다 |
|---|---|
| 빌드 에러 | git 조작 (커밋·머지·revert·충돌) |
| 조용히 안 먹는 설정 | 단순 오타, 즉시 알아챈 실수 |
| 설계 오류 | 원인을 못 찾은 것 |
| 사용자가 지적한 문제 | 에디터 사용법 질문 |
| ⭐ **레퍼런스 대조로 찾은 결함** | |

## 왜 남기나

같은 실수를 두 번 하지 않기 위해서다. 특히 **엔진이 조용히 삼키는 종류**의 문제는
기록이 없으면 몇 달 뒤에 똑같이 하루를 쓴다.

## 목록 (최신순)

| # | 제목 | 증상 한 줄 | 발견 | 상태 |
|---|---|---|---|---|
| [E07](E07_클릭이동_서버구동_충돌.md) | 서버가 이동을 직접 구동하면 충돌 | 이동이 끊기거나 튄다 | 구현 중 발견 | ✅ 설계 변경으로 회피 |
| [E06](E06_SetByCaller_DataName_편집불가.md) | SetByCaller `Data Name` 이 편집 불가 | 에디터에서 `None` 에서 안 바뀜 | ⭐ **사용자 지적** | ✅ 해결 |
| [E05](E05_PostAttributeChange_클라권위.md) | `PostAttributeChange` 클라 권위 누락 | 클라가 자기 어트리뷰트 베이스를 고침 | Lyra 대조 | ✅ 구현 전 수정 |
| [E04](E04_사망알림_중복발동.md) | 사망 알림이 여러 번 발동 | 체력 0 인 대상을 또 때리면 킬 로그가 **중복** | Lyra 대조 | ✅ 구현 전 수정 |
| [E03](E03_팀조회_컨트롤러경유.md) | 팀 조회를 Controller 경유로 설계 | 클라에서 **적의 팀을 못 읽는다** | 구현 중 | ✅ 구현 전 수정 |
| [E02](E02_FOverlapResult_불완전타입.md) | `FOverlapResult` 불완전 타입 | `error C2027` — UE5에서 `World.h` 가 안 끌어옴 | 빌드 | ✅ 해결 |
| [E01](E01_스킬판정_채널응답_무시됨.md) | 스킬 판정 채널 응답이 무시됨 | C++로 켠 `Overlap` 이 BP에서 `Ignore` 로 보임 | ⭐ **사용자 지적** | ✅ 해결 |

## 기록 규칙

- 파일명: `E##_짧은제목.md` — 번호는 1부터 증가
- **반드시 포함**: 증상 / 원인(근거 포함) / 해결 / 재발 방지
- ⭐ **원인에 근거를 단다.** 엔진 소스는 `파일:라인`, 로그는 원문 인용
- 원인을 못 찾았으면 **`(원인 미확인)`** 이라고 적는다. 추측을 원인처럼 쓰지 않는다
- 고친 뒤 **어떻게 재발을 막는지** 적는다. 못 막으면 "못 막는다"고 적는다
