# ✅ F00 — GAS 기반 세팅 · 기능 완료 판정

> Task [`../../Task/F00_GAS기반세팅/00_개요.md`](../../1_Task/F00_GAS기반세팅/00_개요.md)
> ⚠ **모든 기능이 이 위에 선다.** 여기가 흔들리면 전부 흔들린다.

## 세부 Task 완료

- [x] [01 플러그인 · 모듈 확인](01_플러그인_모듈확인.md)
- [x] [02 ASC 배치 결정](02_ASC_배치결정.md) ⭐
- [x] [03 ASC 부착 · 초기화](03_ASC_부착_초기화.md)
- [x] [04 복제 모드 설정](04_복제모드_설정.md)
- [x] [05 GameplayTag 체계](05_GameplayTag_체계.md)

## 빌드 (1차 검증 — 생략 불가)

- [x] `EternalReturnEditor` 빌드 통과
- [x] `EternalReturnServer` 빌드 통과
- [x] 빌드 액션 수가 **수천 개가 아니다** (수천이면 엔진 전체 재빌드 — 사용자에게 알린다)

## 통합 검증 — `Play As Client, Number of Players: 2`

- [x] ⭐ **서버에서** `GetAbilitySystemComponent()` 가 유효한 포인터를 반환
- [x] ⭐ **클라에서도** 유효한 포인터를 반환
- [x] `AbilityActorInfo->AvatarActor` 가 캐릭터를 가리킨다 (양쪽)
- [x] `AbilityActorInfo->OwnerActor` 가 PlayerState를 가리킨다
- [ ] 체력 변화가 **즉시** 반영된다 (초당 1회 지연이 아니다)
- [ ] 태그를 문자열 없이 코드에서 참조할 수 있다

## ⭐ 되돌리기 어려운 결정 — 기록됐는가

- [x] 플레이어 ASC 위치를 **문서에 기록**했다 (F00-02 결정 표)
- [x] 야생동물은 Pawn에 붙는다는 예외를 기록했다
- [x] `IAbilitySystemInterface` 로 접근을 통일했다
- [x] 복제 모드(플레이어 `Mixed` / AI `Minimal`)를 기록했다

## ⭐ UE 5.4 함정 — 인터넷 예제를 따라 하지 않았는가

- [x] **`InitGlobalData()` 를 직접 호출하지 않았다**
  ```
  grep -rn "InitGlobalData" Source/    ← 아무것도 안 나와야 함
  ```
  근거: `AbilitySystemGlobals.cpp:68` — *"we call InitGlobalData automatically in UE5.3+"*
- [x] 플레이어 ASC에 `Minimal` 을 쓰지 않았다 (`AbilitySystemComponent.h:87` 이 금지)

## 흔한 실수 — 전체 점검

- [x] ASC `UPROPERTY()` 를 붙였다 (GC dangling 방지)
- [x] ASC 복제를 켰다 (`SetIsReplicated(true)`)
- [x] `InitAbilityActorInfo` 를 **서버·클라 양쪽**에서 부른다
- [x] `APlayerState::NetUpdateFrequency` 를 올렸다 (기본 1)
- [x] 태그를 코드에 문자열로 박지 않았다

## 미해결로 남긴 것

| 항목 | 상태 |
|---|---|
| `NetUpdateFrequency` 적정값 | **(미측정)** — 100은 자체 결정 초기값 |
| 24명 규모 GAS 복제 총비용 | **(미측정)** |
| 부활 시 어트리뷰트 초기화 범위 | (미확인) — F14와 함께 |
| 관전 상태의 ASC 처리 | (미확인) |

## 다음으로

- [x] F00이 끝나야 F02(어트리뷰트셋)를 시작할 수 있다
- [x] F01(코어 골격)과 F04(판정)는 F00과 **병행 가능**하다

---

## 실행 기록 (2026-09-06)

### 만든 것

```
Source/EternalReturn/GAS/ERGameplayTags.h / .cpp        네이티브 태그 22개
Source/EternalReturn/Core/ERPlayerState.h / .cpp        ASC 소유 + Mixed + NetUpdateFrequency
Source/EternalReturn/Core/ERGameMode.h / .cpp           검증용 배선
Source/EternalReturn/Character/ERCharacterBase.h / .cpp InitAbilityActorInfo 서버·클라
```

### 빌드

| 타겟 | 결과 |
|---|---|
| `EternalReturnEditor` | ✅ **exit 0** · 액션 12개 · 56초 · 에러/경고 0 · UHT 생성 8개(프로젝트 모듈만) |
| `EternalReturnServer` | ✅ **exit 0** · 에러/경고 0 · `EternalReturnServer.exe` 생성 확인 |

### ⭐ 엔진에서 확인한 사실 (추측 아님)

- ASC 생성자의 기본 복제 모드는 **`Full`** 이다. 안 바꾸면 24명분 활성 이펙트가 전원에게 간다
- `Minimal` 은 소유자 있는 ASC 에서 동작하지 않는다 (`AbilitySystemComponent.h:87`)
- `InitGlobalData()` 는 UE 5.3+ 자동 — 호출하지 않았다 (`grep` 0건)

### ⬜ 남은 것 — 전부 에디터 작업이라 코드로 못 한다

[`../../EditorTasks/F00_GAS기반세팅.md`](../../3_EditorTasks/F00_GAS기반세팅.md) 로 넘겼다.

원인: `Config/DefaultEngine.ini:4` 의 `GlobalDefaultGameMode` 가 **부모 클래스가 삭제된
`BP_RGameMode`** 를 가리킨다. 그대로 PIE 를 켜면 `AERGameMode`/`AERPlayerState` 가 쓰이지 않는다.

- 통합 검증 6건 (`Play As Client, Number of Players: 4`)
- 에디터 플러그인 목록 / 태그 목록 표시 확인
- `NetUpdateFrequency` 실측

> ⚠ **서버 타겟 최초 빌드는 엔진 모듈 794개를 서버 구성으로 새로 컴파일한다.**
> 이 환경에서 10분 타임아웃에 5회 걸쳐 나눠 돌렸다(794 → 450 → 311 → 205 → 완료).
> 매 회차 에러 0건이었고 오브젝트가 캐시되어 이어서 진행됐다. **실패가 아니라 분량 문제였다.**
> 다음부터는 프로젝트 모듈만 다시 빌드하므로 수십 초로 끝난다.

---

## PIE 검증 (2026-09-06 14:34)

✅ **F00 의 핵심 판정 통과** — 서버·클라 양쪽에서 `InitAbilityActorInfo` 가 호출되고
`Owner`=PlayerState / `Avatar`=Character 로 짝이 맞는다. 상세는 [03 체크리스트](03_ASC_부착_초기화.md) 하단.

에디터 쪽 조치 (사용자 수행):
- `Config/DefaultEngine.ini:4` → `GlobalDefaultGameMode=/Game/Core/BP_ERGameMode.BP_ERGameMode_C`
- `BP_ERCharacterBase` 가 `AERCharacterBase` 를 부모로 사용 (로그의 `BP_ERCharacterBase_C_*` 로 확인)

### ⚠ 검증 직후 소스가 한때 깨졌다가 복구됨

git 작업(커밋 → 재커밋 → `Revert`) 중 소스 6개 파일에 **충돌 마커가 들어간 시점이 있었다.**
현재는 작업 트리가 깨끗하고 마커도 없으며, **확인용 증분 빌드 통과**(exit 0, 35초, 9액션).

> 되돌아온 파일이 옛 삭제 코드(카메라·SpringArm·`AERPlayerController` 참조)와 섞이면
> 빌드가 깨진다. **git 작업 후에는 항상 `grep -rn "^<<<<<<<" Source/` 로 확인한다.**

### 아직 남은 것

- ⬜ **체력 변화 즉시 반영** — 어트리뷰트가 없어 확인 불가. **F02 에서 검증한다**
- ⬜ **태그 문자열 없이 참조** — 사용처가 없어 확인 불가. **F03/F06 에서 검증한다**
- ⬜ 부활 후 Avatar 재지정 — 부활 시스템(F14) 이후
- ⬜ `NetUpdateFrequency` 실측 — 봇 24명 필요
