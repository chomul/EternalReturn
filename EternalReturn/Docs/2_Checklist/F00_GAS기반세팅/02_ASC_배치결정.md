# ✅ F00-02 — ASC 배치 결정 ⭐

> Task [`../../Task/F00_GAS기반세팅/02_ASC_배치결정.md`](../../1_Task/F00_GAS기반세팅/02_ASC_배치결정.md)
> ⚠ **되돌리기 비용이 가장 큰 결정이다.** 코드가 쌓인 뒤에는 F02·F03·F07을 다 고쳐야 한다.

## 선행

- [x] [01 플러그인 · 모듈 확인](01_플러그인_모듈확인.md) 완료
- [x] Task 문서의 A/B 장단점을 읽었다

## 결정

- [x] 플레이어 ASC 위치를 정했다 → **권장: `AERPlayerState`**
- [x] 근거를 이해했다: **이 게임은 부활이 코어 루프에 있다** (자동 → 크레딧 200 → 불가 3단)
- [x] 야생동물·보스는 **Pawn** 에 붙는다는 예외를 확인했다
- [x] `IAbilitySystemInterface` 로 접근을 통일하기로 했다

## 결정에 따라오는 것 — 빠뜨리면 나중에 아프다

PlayerState를 골랐다면:

- [x] ⭐ `NetUpdateFrequency` 를 올려야 한다 (기본 **1**, `PlayerState.cpp:26`) → F00-04
- [x] ⭐ 복제 모드를 `Mixed` 로 해야 한다 (`bAlwaysRelevant = true`, `:27`) → F00-04
- [x] Pawn → ASC 접근이 한 단계 늘어남을 감안했다

Pawn을 골랐다면: — ⛔ **해당 없음.** PlayerState 를 골랐다

- [~] ⭐ 부활 시 어트리뷰트 복원 코드가 필요하다 — 그 비용을 감수하기로 했다
- [~] 어디서 복원할지 정했다

## 기록

- [x] Task 문서 하단의 「결정 기록」 표를 채웠다
- [x] 또는 코드 주석에 결정과 근거를 남겼다
- [x] ⭐ **"이 결정을 바꾸려면 F02·F03·F07을 함께 고쳐야 한다"** 는 경고를 남겼다

## 인벤토리 배치와의 관계

- [x] [네트워크 문서 §2.3](../../0_GameDesign/Systems/네트워크_서버아키텍처_역기획서.md)의 인벤토리 미결 건과 **같은 판단 기준**임을 확인했다
- [x] ⭐ **여기서 인벤토리까지 정하지 않았다** — F08에서 이 결정을 근거로 다시 판단한다
- [x] 인벤토리는 `COND_OwnerOnly` 가 필수라는 점을 기록했다 (정보 유출)

## 이 Task에는 빌드가 없다

- [x] 실제 부착과 빌드는 [03 ASC 부착 · 초기화](03_ASC_부착_초기화.md) 에서 한다

---

## 실행 기록 (2026-09-06)

**결정: 플레이어 ASC 는 `AERPlayerState`.** 근거와 경고를 `Source/EternalReturn/Core/ERPlayerState.h` 클래스 주석에 남겼다.

- Pawn 이 아닌 이유 — 부활이 3단(자동 → 크레딧 200 → 불가)이라 죽을 때마다 어트리뷰트를 손으로 복원해야 하고, 그 코드가 성장·인벤토리와 얽히며 자란다
- 야생동물·보스는 PlayerState 가 없어 **Pawn 에 붙는다** → 두 배치가 공존
- `AERCharacterBase` 도 `IAbilitySystemInterface` 를 구현해 PlayerState 의 ASC 를 넘긴다 → **호출부는 배치를 모른다**
- ⚠ **인벤토리 배치는 정하지 않았다.** F08 에서 이 결정을 근거로 판단한다
