# ✅ F06-03 — 기절 · 채널링 취소 ⭐

> Task [`../../1_Task/F06_상태이상_CC/03_기절_채널링취소.md`](../../1_Task/F06_상태이상_CC/03_기절_채널링취소.md)

## 선행

- [ ] [01 CC GE 기반](01_CC_GE기반_차단배선.md) 완료

## 구현

- [x] ~~CC GE 에 `CancelAbilitiesWithTag = Ability.Form.Channeled` 를 설정~~ → ⚠ **그 기능은 GE 에 없다** (GE 는 `BlockAbilitiesWithTags` 만, `GameplayEffect.cpp:4279`). F07-04 에서 **어빌리티가 `State.Block.Skill` 부여를 감시**해 스스로 끊는 방식으로 대체 ([`../../4_Argument/17_시전상태_표현과_취소경로.md`](../../4_Argument/17_시전상태_표현과_취소경로.md) ②A). 애셋 설정 불필요
- [x] ⭐ **자체 취소 상태 머신을 만들지 않았다** — `UAbilityTask_WaitGameplayTagAdded`
- [x] ⚠ 취소 시 **쿨다운을 되돌리지 않는다** (설계다) — `CancelCast` 가 `CommitAbilityCooldown(Force)` 로 오히려 건다

## 빌드

- [ ] `EternalReturnEditor` 빌드 통과
- [ ] `EternalReturnServer` 빌드 통과

## 검증 — `Play As Client, Number of Players: 2`

- [x] ⭐ 채널링 중 기절 → **시전이 취소된다** — F07-04 에서 확인 (2026-09-13, `State.Block.Skill` 부여 → 같은 프레임 취소)
- [x] 취소돼도 **쿨다운이 남는다** — F07-04 에서 확인 (`CommitAbilityCooldown` 후 취소)
- [x] 기절 만료 후 다시 시전할 수 있다 — `ER.CC.Clear` 후 재시전 (사용자 확인)
- [x] 즉발 스킬은 영향받지 않는다 — CastTime 0 이면 감시 태스크 자체가 없다 (코드) · F07-01 에서 즉발 Q 가 기절에 **차단**되는 것은 별도 확인

## ⭐ 가장 위험한 실수

- [x] ⭐ **자체 취소 로직을 만들지 않았다** — GAS 태스크(`WaitGameplayTagAdded`) + `CancelAbility`
- [ ] ⚠ 취소 시 쿨다운을 환불하지 않았다
- [x] ~~채널링 어빌리티가 `Ability.Form.Channeled` 를 갖는지~~ → 태그 불필요. `CastTime > 0` 이면 베이스가 감시한다. 애셋 태그를 빠뜨려서 조용히 안 끊기는 실수 자체가 없다
