# ✅ F02-05 — 초기값 데이터 테이블

> **2026-09-09 검증 완료** — 사용자 확인: *"테스트 완료했어 오류 없음"*
> 절차: `Docs/3_EditorTasks/F02_어트리뷰트셋.md` ③ 절

> Task [`../../Task/F02_어트리뷰트셋/05_초기값_데이터테이블.md`](../../1_Task/F02_어트리뷰트셋/05_초기값_데이터테이블.md)

## 선행

- [x] [04 체력 처리 · 사망 알림](04_체력처리_사망알림.md) 완료

## 구현

- [x] 스탯 구조체 `FERCharStats` 정의
  ⚠ Task 문서의 `FCharStatRow` → `FERCharStatRow` → **`FERCharStats`** 로 두 번 바뀌었다.
  `ER` 프리픽스(`CLAUDE.md` §0) + **데이터 테이블 행이 아니게 됨**
  ([`../../4_Argument/4_스탯데이터_저장방식.md`](../../4_Argument/4_스탯데이터_저장방식.md))
- [x] ⭐ 실험체별 데이터 애셋 `UERCharacterData : UPrimaryDataAsset` 정의
- [x] 캐릭터가 `TObjectPtr<UERCharacterData>` 로 참조한다 (행 핸들 아님)
- [x] ~~RowName = 실험체 이름~~ → **폐기.** 애셋 하나가 실험체 하나다
- [x] 필드가 `UPROPERTY(EditDefaultsOnly)`
- [x] ⭐ **초기화를 GE로 한다** — 코드에서 `SetHealth()` 직접 호출 안 함
- [x] 초기화가 **서버에서만** 실행된다
- [x] ⭐ **MaxHealth 를 먼저 세팅**하고 Health 를 채운다
- [x] 방식 A(Instant GE + SetByCaller)를 골랐다면 그 근거를 주석에 남겼다

## 빌드

- [x] `EternalReturnEditor` 빌드 통과
- [x] `EternalReturnServer` 빌드 통과

## 검증

> ✅ **PIE 검증 완료 (2026-09-08).**
> 절차와 성공 판정: [`../../3_EditorTasks/F02_어트리뷰트셋.md`](../../3_EditorTasks/F02_어트리뷰트셋.md) ② 테스트

- [x] 캐릭터 스폰 시 어트리뷰트가 테이블 값으로 초기화된다
- [x] `Health == MaxHealth` 로 시작한다
- [x] ⭐ **클라에서도 초기값이 보인다**
- [x] 테이블 값을 바꾸면 반영된다 (하드코딩 아님)

## ⭐ 가장 흔한 순서 실수

- [x] **초기화가 `InitAbilityActorInfo`(F00-03) 이후**에 일어난다 — 그 전에 GE를 걸면 조용히 실패한다
- [x] **MaxHealth가 Health보다 먼저** 세팅된다 — 뒤집히면 체력이 0으로 클램프된다

## 흔한 실수 — 걸렸는지 확인

- [x] 어트리뷰트를 코드에서 직접 세팅하지 않았다 (클램프·`PostGameplayEffectExecute` 우회 방지)
- [x] ⭐ `FERCharStats` 필드 이름을 신중히 정했다 — **바꾸면 데이터 테이블 값이 날아간다**
  필드 이름이 곧 SetByCaller 키라서, 바꾸면 GE 애셋의 모디파이어도 같이 끊긴다. 헤더 주석에 명시
  ⭐ **이름을 바꿀 수 있었던 건 애셋을 아직 안 만들었기 때문이다.** 만든 뒤였다면 못 바꾼다
- [x] 클라에서 초기화하지 않았다

## 에디터 작업

- [x] ⭐ GE 애셋 · 데이터 테이블 애셋을 **직접 만들지 않았다** (`Content/` 금지)
- [x] `Docs/3_EditorTasks/` 문서로 넘겼다
- [x] 문서에 애셋 경로 · 부모 클래스 · 프로퍼티 · 예시 값을 적었다

## 미확인으로 남긴 것

- [x] ⭐ **실험체별 기본 스탯 실제 수치는 (미확인)** — 임시값이며 인게임 계측 필요임을 표기했다
- [x] 부활 시 재초기화 범위는 F14와 함께 정하기로 남겼다
