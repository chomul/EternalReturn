# ✅ F02-06 — 변경 델리게이트

> **2026-09-09 검증 완료** — 사용자 확인: *"테스트 완료했어 오류 없음"*
> 절차: `Docs/3_EditorTasks/F02_어트리뷰트셋.md` ③ 절

> Task [`../../Task/F02_어트리뷰트셋/06_변경_델리게이트.md`](../../1_Task/F02_어트리뷰트셋/06_변경_델리게이트.md)

## 선행

- [x] [05 초기값 데이터 테이블](05_초기값_데이터테이블.md) 완료

## 구현

- [x] ⭐ **자체 멀티캐스트 델리게이트를 만들지 않았다** — ASC 의 `GetGameplayAttributeValueChangeDelegate()` 를 쓴다
  ⚠ `FOnOutOfHealth` 는 있지만 그건 **사망 알림**이지 어트리뷰트 변경 델리게이트가 아니다
- [x] 사망 알림 델리게이트를 노출했다 (F02-04에서 만든 것)
- [x] 구독 방법을 **문서화**했다 — `ERAttributeSet.h` 의 델리게이트 아래 주석 블록
  구독 예시 · 구독 시점 · 해제 · UMG Property Binding 금지 이유까지
- [x] 테스트용 임시 구독으로 동작을 확인했다 — `ER.Attr.Watch` / `ER.Attr.Unwatch`
  ⬜ PIE 확인 대기

## 빌드

- [x] `EternalReturnEditor` 빌드 통과
- [x] `EternalReturnServer` 빌드 통과

## 검증

> ⬜ **확인 대기.** 절차: [`../../3_EditorTasks/F02_어트리뷰트셋.md`](../../3_EditorTasks/F02_어트리뷰트셋.md) **③ 절** — `Play As Client, Number of Players: 2`

- [x] ⭐ 서버에서 GE로 체력을 바꾸면 **클라에서 델리게이트가 발동**한다
- [x] 체력만 구독한 리스너가 **공격력 변화에는 안 깨어난다**
- [x] 체력 0 시 사망 델리게이트가 발동한다
- [x] ⭐ **`Tick` 없이 동작한다** — `bCanEverTick = false`

## ⭐ UI 비의존 확인 — 이게 이 Task의 핵심

- [x] 위젯/UMG/HUD 를 아는 코드가 **한 줄도 없다** — `grep -c "Widget|UMG|HUD" ERAttributeSet.*` → **0**
  ```
  grep -rniE "widget|umg|hud" Source/EternalReturn/GAS/ERAttributeSet.*   ← 안 나와야 함
  ```
- [x] **구독은 듣는 쪽이 한다** — AttributeSet이 구독자를 등록하지 않는다
- [x] UMG Property Binding 을 쓰지 않기로 했다 (매 프레임 호출됨) — 헤더 주석에 명시

## 흔한 실수 — 걸렸는지 확인

- [x] 구독 시점이 ASC 초기화 이후다 — 헤더 주석에 명시. 검증 커맨드도 그 이후에만 동작한다
- [x] ⭐ **구독 해제**를 한다 — 안 하면 dangling 델리게이트
  ⚠ 처음 구현은 `RemoveAll(nullptr)` 이라 **실제로 해제가 안 됐다.**
  `AddStatic` 은 바인딩 객체가 없어 **핸들(`Remove(Handle)`)로만** 지워진다. 고쳤다.
  ⬜ PIE 로 "해제 후 로그가 안 나오는지" 확인 대기
- [x] 테스트용 임시 구독 코드의 **제거 경로가 확보돼 있다**
  ⭐ `ERDamageDebug.cpp` 안에 넣었다 — 그 파일은 **F07 에서 통째로 삭제**한다.
  지울 코드를 새로 만들지 않았다
- [x] 어트리뷰트 델리게이트에 맥락을 억지로 싣지 않았다
  ⚠ **사망 델리게이트는 예외로 `Instigator` 를 싣는다** — 킬 크레딧은 그것 없이 불가능하다

## 판단 기록

- [x] 사망 델리게이트 시그니처를 F03 뒤에 확정했다 — **`Instigator` 1인자 유지**
  구독자가 0명인 지금이 변경 비용이 가장 싸다. `EffectCauser`(무기 아이콘용)는
  **F14 가 실제로 요구하면** 그때 늘린다 (`CLAUDE.md` §2)
