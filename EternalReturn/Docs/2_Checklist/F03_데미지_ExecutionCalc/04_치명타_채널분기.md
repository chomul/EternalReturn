# ✅ F03-04 — 치명타 · 채널 분기 ⭐

> **2026-09-08 검증 완료** — 사용자 확인: *"테스트 완료했어 오류 없음"*
> ⬜ `GameplayCue` 전달만 남았다 (듣는 쪽이 없어 의도적으로 미룸)

> Task [`../../Task/F03_데미지_ExecutionCalc/04_치명타_채널분기.md`](../../1_Task/F03_데미지_ExecutionCalc/04_치명타_채널분기.md)

## 선행

- [x] [03 방어 관통](03_방어관통.md) 완료
- [x] [F00-05 GameplayTag 체계](../F00_GAS기반세팅/05_GameplayTag_체계.md) 완료 — 채널 태그가 있어야 한다

## 구현

- [x] 채널을 **GameplayTag로 판별** (`Damage.Type.BasicAttack` / `Skill` / `True`)
- [x] ⭐ **채널 검사가 조건문 맨 앞**에 있다
- [x] 기본 공격에서만 치명타 판정
- [x] 기본 배율 **175%**
- [x] ⭐ **태그 없는 GE에 경고 로그**
- [ ] 치명타 여부를 `GameplayCue` 로 전달할 방법을 마련했다
  ⬜ **아직 안 만들었다.** 지금은 듣는 쪽(UI·연출)이 없어 만들 근거가 없다 (`CLAUDE.md` §2).
  방법은 확인해 뒀다 — Lyra 는 `FGameplayEffectContext` 를 상속해 플래그를 싣는다
  (`LyraGameplayEffectContext`, `LyraDamageExecution.cpp:38-40` 이 `ExtractEffectContext` 로 꺼낸다).
  **F17(UI) 또는 연출 작업 때** 같은 방식으로 붙인다
- [x] ⭐ **Execution 안에서 이펙트를 재생하지 않는다**
- [x] 태그를 문자열로 박지 않았다 (F00-05 상수 사용)

## 빌드

- [x] `EternalReturnEditor` 빌드 통과
- [x] `EternalReturnServer` 빌드 통과

## 수치 검증

> ✅ **검증 완료 (2026-09-08).** 절차: [`../../3_EditorTasks/F03_데미지_ExecutionCalc.md`](../../3_EditorTasks/F03_데미지_ExecutionCalc.md) **2-6 절**

- [x] ⭐ **스킬 태그 + 치명타 확률 100% → 치명타 발생 안 함** (문서 §7 4순위 기준)
- [x] ⭐ **고정 피해 태그 + 확률 100% → 치명타 발생 안 함**
- [x] 기본 공격 태그 + 확률 100% + 175% → **1.75배**
- [x] 기본 공격 태그 + 확률 0% → 항상 기본 피해
- [x] 기본 공격 태그 + 확률 50% × 1000회 → 대략 절반
- [x] ⭐ **태그 없음 → 경고 로그가 남는다**

## ⭐ GAS에서 더 위험해진 지점

자체 구현의 `enum` 은 값을 안 넣으면 컴파일이 걸렸다. **태그는 안 붙여도 조용히 통과한다.**

- [x] 태그 누락을 **조용히 넘기지 않는다**
- [x] 어느 분기에도 안 들어간 경우를 명시적으로 처리했다

## 흔한 실수 — 걸렸는지 확인

- [x] ⭐ **확률을 먼저 계산하고 채널을 나중에 보지 않았다** — 순서가 뒤집히면 스킬에 치명타가 붙는다
- [x] 치명타 적용 순서 — **방어력 감산 뒤**. 주석에 남겼다
  ⚠ 자체 결정이 아니라 계산식 §2.2 가 6 단계를 5 단계 뒤에 뒀다
- [x] 확률 0%/100% 로 검증했다 (`FMath::FRand()` 는 비결정적)
- [x] 치명타 확률 상한을 여기서 또 자르지 않았다 (F02-03 소관)

## ⚠ 이 항목은 밸런스의 뼈대다

- [x] 스킬에 치명타가 안 붙는다는 것을 **F07 에서 다시 확인**할 회귀 항목으로 기록했다
  → [`../../3_EditorTasks/F03_데미지_ExecutionCalc.md`](../../3_EditorTasks/F03_데미지_ExecutionCalc.md) 의
  「아직 못 하는 검증」에 남긴다. 실제 스킬 GE 가 생기면 태그가 제대로 붙는지 다시 본다
