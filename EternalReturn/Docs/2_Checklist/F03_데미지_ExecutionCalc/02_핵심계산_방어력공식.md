# ✅ F03-02 — 핵심 계산 · 방어력 공식 ⭐

> Task [`../../Task/F03_데미지_ExecutionCalc/02_핵심계산_방어력공식.md`](../../1_Task/F03_데미지_ExecutionCalc/02_핵심계산_방어력공식.md)
> ⭐ **문서 §7: "2순위가 이 문서의 전부다."**

## 선행

- [x] [01 어트리뷰트 캡처 정의](01_어트리뷰트_캡처정의.md) 완료

## 구현

- [x] `UERDamageExecution : public UGameplayEffectExecutionCalculation`
- [x] `Execute_Implementation` 오버라이드
- [x] 방어력 모델 **`100 / (100 + DEF)`**
- [x] ⭐ ~~원시 피해를 SetByCaller 로 받는다~~ → **계수를 SetByCaller 로 받는다**
  Task 문서 스케치는 어빌리티가 완성된 원시 피해를 넘기는 형태였다. 그러면
  `AttemptCalculateCapturedAttributeBonusMagnitude` 가 Execution 전용 API 라
  **추가 공격력을 읽을 수 없다**(`GameplayEffectExecutionCalculation.h:95`).
  → `Data.Damage.Base/APRatio/BonusAPRatio/SkillAmpRatio` 4개로 계수만 받고
  곱셈은 Execution 이 한다. 근거: [`../../4_Argument/5_추가공격력_산출방식.md`](../../4_Argument/5_추가공격력_산출방식.md)
- [x] 고정 피해 태그면 방어력을 **적용하지 않는다**
- [x] ⭐ 결과를 **`IncomingDamage` 출력 수정자**로 낸다
- [x] `FMath::Max(Defense, 0.f)` 로 분모 보호
- [x] 「최종 피해 추가(%)」를 **적용하지 않았다** (해석 미확인)

## 빌드

- [x] `EternalReturnEditor` 빌드 통과
- [x] `EternalReturnServer` 빌드 통과

## 수치 검증

> ✅ **검증 완료 (2026-09-08)** — 사용자 확인: *"테스트 완료했어 오류 없음"*
>
> 수단: `ER.Damage.Test` / `ER.Damage.BonusTest` 콘솔 커맨드를 넣었다.
> 절차: [`../../3_EditorTasks/F03_데미지_ExecutionCalc.md`](../../3_EditorTasks/F03_데미지_ExecutionCalc.md) ② 테스트
> ⚠ 사용자가 PIE 로 확인해야 채울 수 있다.

- [x] 원시 200, 방어력 100, 스킬 → ⭐ **정확히 100**
- [x] 원시 200, 방어력 0 → 200
- [x] 원시 200, 방어력 100, **고정 피해 태그** → **200**
- [x] 원시 200, 방어력 300 → 50
- [x] 방어력이 음수여도 크래시하지 않는다

## ⭐ 책임 분리 확인

- [x] **체력을 직접 건드리지 않는다** — `IncomingDamage` 에 출력만
- [x] **클램프하지 않는다** (F02-03 소관)
- [x] **이펙트·사운드·로그가 없다** (`GameplayCue` 소관)
- [x] 어빌리티가 계산식을 모른다 — **"몇 배"만 넘긴다**
  ⭐ 스탯을 읽는 코드가 `ERDamageExecution` 하나뿐이다

## 흔한 실수 — 걸렸는지 확인

- [x] 정수로 떨어지는 케이스로 검증했다 (부동소수 비교 아님)
- [x] ⭐ **「최종 피해 추가」를 추측해서 적용하지 않았다** — `(1+x)` 인지 `x` 인지 모른다
- [x] `EGameplayModOp::Additive` 로 출력했다
- [x] 이 파일에 로그·이펙트를 섞지 않았다 (GAS 이관성과 무관하게, 단일 책임)

## 에디터 작업

- [x] 이 Execution을 쓰는 **GE 애셋이 필요**하다
- [x] 애셋 생성을 직접 하지 않고 `Docs/3_EditorTasks/` 로 넘겼다
  → [`../../3_EditorTasks/F03_데미지_ExecutionCalc.md`](../../3_EditorTasks/F03_데미지_ExecutionCalc.md)
  ⭐ 결과적으로 **에디터 작업은 "없음"** 이다. 검증용 GE 는 디버그 코드가 런타임에 만든다

## 막혔을 때

| 증상 | 확인할 것 |
|---|---|
| 피해가 0 | SetByCaller 태그 이름 불일치, 또는 `RelevantAttributesToCapture` 누락 |
| 피해가 예상보다 큼 | 「최종 피해 추가」를 적용했는지, 채널 태그가 `True` 인지 |
| 체력이 안 줄어듦 | F02-04의 `PostGameplayEffectExecute` 가 `IncomingDamage` 를 읽는지 |
