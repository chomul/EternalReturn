# ✅ F04-01 — 타입 정의 · 구조체

> Task [`../../Task/F04_판정_타게팅/01_타입정의_구조체.md`](../../Task/F04_판정_타게팅/01_타입정의_구조체.md)

## 선행

- [ ] 없음 (F01~F03과 독립)

## 구현

- [ ] `ESkillTargeting` 6종 정의
- [ ] `ETargetTeamFilter` 3종 (`Enemy` / `Ally` / `All`)
- [ ] `FTargetQuery` 정의
  - [ ] `RangeMax` / ⭐ **`RangeMin`** (시셀라 Q 하한 1.0)
  - [ ] ⭐ `RadiusInner` / `RadiusOuter` (레니 W)
  - [ ] `AngleDeg` (다니엘 Q 65)
  - [ ] `bPenetrate`
  - [ ] ⭐ `InstigatorTeamId` (호출자가 채운다)
  - [ ] `IgnoredActors`
- [ ] `FTargetResult` 정의
  - [ ] `HitActors`
  - [ ] ⭐ `InnerHitActors` (이중 반경용)
  - [ ] ⭐ `Distances` (거리 보간용)
- [ ] ⭐ **`TArray<AActor*>` 에 `UPROPERTY()`** 를 붙였다
- [ ] 모든 필드에 기본값이 있다
- [ ] 단위가 **m** 임을 필드 주석에 명시했다

## 빌드

- [ ] `EternalReturnEditor` 빌드 통과
- [ ] `EternalReturnServer` 빌드 통과

## ⭐ 독립성 검증

- [ ] 헤더에 게임플레이 헤더 include가 없다
  ```
  grep -n "#include" Source/EternalReturn/Combat/ERTargetingTypes.h
  ```
- [ ] 구조체를 **손으로 채워 만들 수 있다**

## 흔한 실수 — 걸렸는지 확인

- [ ] ⭐ **`RangeMin` 을 나중에 끼우지 않고 처음부터 넣었다** (시셀라 Q가 요구)
- [ ] ⭐ **`UPROPERTY()` 누락으로 GC dangling 위험이 없다**
- [ ] 열거형 값을 중간에 끼워 넣지 않는다 (끝에 추가)
- [ ] `Distances` 를 결과에 넣었다 (거리를 두 번 계산하지 않기 위해)
- [ ] `InnerHitActors` 를 따로 뒀다 (호출자가 다시 갈라내지 않게)

## 판단 기록

- [ ] 판정 기준점을 무엇으로 정할지 F04-02에서 결정할 항목으로 남겼다
- [ ] 단위 변환(m → uu) 지점을 어디에 둘지 정했다
