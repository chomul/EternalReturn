# ✅ F04-03 — 방향 투사체

> Task [`../../Task/F04_판정_타게팅/03_방향투사체.md`](../../Task/F04_판정_타게팅/03_방향투사체.md)

## 선행

- [ ] [02 대상 지정 · 자기중심 원형](02_대상지정_자기중심원형.md) 완료 (공통 규칙 확정됨)

## 구현

- [ ] `QueryProjectile()` 정적 함수
- [ ] `bPenetrate == false` → 첫 대상만
- [ ] `bPenetrate == true` → 경로상 전부, 가까운 순
- [ ] ⭐ `Direction` 을 **함수 안에서 정규화**한다
- [ ] ⭐ `RangeMin` 안쪽 대상 제외
- [ ] 판정 두께를 정하고 **자체 결정값으로 주석 표기**
- [ ] 벽 차폐 여부를 **인자로 열어뒀다** (레이피어 D = 벽 통과)
- [ ] ⭐ **액터를 스폰하지 않는다**

## 빌드

- [ ] `EternalReturnEditor` 빌드 통과
- [ ] `EternalReturnServer` 빌드 통과

## 검증

- [ ] 비관통 — 일직선 3명 중 **가장 가까운 1명만**
- [ ] 관통 — 3명 전부, **가까운 순**
- [ ] 사거리 밖 대상 제외
- [ ] ⭐ `RangeMin` 1.0m 안쪽 대상 **제외** (시셀라 Q)
- [ ] `Direction` 이 정규화되지 않아도 동작
- [ ] 아무도 없을 때 빈 배열
- [ ] 벽 차폐 on/off가 각각 동작

## ⭐ 책임 경계 확인

- [ ] 날아가는 투사체 액터를 **여기서 만들지 않는다** (F07 소관)
- [ ] 이 함수는 **"직선상에 누가 있나"만** 답한다
  ```
  grep -n "SpawnActor" Source/EternalReturn/Combat/ERTargeting.cpp    ← 안 나와야 함
  ```

## 흔한 실수 — 걸렸는지 확인

- [ ] 영벡터 정규화로 0으로 나누지 않는다
- [ ] 폭 0인 라인 트레이스로 두고 "잘 안 맞는다"를 방치하지 않았다
- [ ] 관통 최대 대상 수를 정하거나 **(미확인)** 으로 남겼다
