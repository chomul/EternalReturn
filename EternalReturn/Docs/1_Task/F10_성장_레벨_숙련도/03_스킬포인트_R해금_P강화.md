# F10-03 — 스킬 포인트 지급 · R 해금 · P 자동 강화

> 기능 [F10](00_개요.md) · 근거 [`성장_레벨_숙련도_특성_역기획서.md`](../../0_GameDesign/Systems/성장_레벨_숙련도_특성_역기획서.md) §2.1 · [`스킬_프레임워크_역기획서.md`](../../0_GameDesign/Systems/스킬_프레임워크_역기획서.md) §2.1 · §2.3 · 우선순위 2
> 체크리스트 [`../../2_Checklist/F10_성장_레벨_숙련도/03_스킬포인트_R해금_P강화.md`](../../2_Checklist/F10_성장_레벨_숙련도/03_스킬포인트_R해금_P강화.md)

## 할 일

F07-03 이 비워 둔 세 자리를 채운다. **배분 · 검증은 이미 있다** (`AERPlayerState::ServerLevelUpSkill` → `ERSkill::LevelUpSkill`). 여기서는 **지급과 해금 조건**만.

| 자리 | 지금 | F10-03 |
|---|---|---|
| 포인트 지급 | 디버그 `ER.Skill.Points` | 레벨업 훅 → `AddSkillPoints(FLevelExpRow.SkillPointGranted)`. Lv.1 시작 포인트 1 도 여기 |
| R 해금 레벨 | 없음 — 1레벨에도 R 을 찍을 수 있다 | `UERSkillData.MinCharacterLevel : TArray<int32>` (스킬 레벨별 요구 실험체 레벨). **원작 (미확인)** — 인터넷 조사 실패(F07-03 기록) → `[자체]` 로 LoL 관례(6/11/16)를 출발값, 기록 |
| ~~P 자동 강화~~ | — | ⚠ **정정 (사용자 확인 2026-09-18)**: P 는 **1레벨 시작 + 포인트로 찍는다.** 기존 `InitialLevel 1 · MaxLevel 3 · bUsesSkillPoints` 로 끝 — 새 코드 없음 |

⭐ 필드는 **스킬 데이터**에 붙는다 — F07-03 의 사용자 결정("레벨 규칙은 슬롯이 아니라 스킬이 든다")의 연장. 코드에 "R 이면 6레벨" 분기가 없다.

## ✅ 2포인트 문제 (§2.1) — 해소

P 가 포인트를 쓴다: `Q5 + W5 + E5 + R3 + P2 = 20` = 지급 20. `SkillPointGranted` 는 조정용으로만 남는다.

## 검증 — `Play As Listen Server, 2명`

- **빌드 통과**
- ⭐ `AddExp` 로 Lv.2 → 포인트 +1 (로그 `포인트 +1 -> N`) · 클라 `ER.Skill.Levels` 에 반영
- ⭐ `MinCharacterLevel = [6, 11, 16]` 인 R: Lv.5 에서 `LevelUp R` → 거부 "실험체 레벨 6 필요" · Lv.6 → 성공 · Lv.6 에서 두 번째 → 거부(11 필요)
- ⭐ P (`InitialLevel 1 · MaxLevel 3 · bUsesSkillPoints`): 시작 Lv.1 · `LevelUp P` 로 2 → 3 · 4번째 거부
- 클라가 조건을 우회해 요청해도 서버가 거부

## 주의점

- ⭐ 지급 · 해금 판정은 **서버** — `ERSkill::LevelUpSkill` 에 실험체 레벨 인자가 하나 늘어난다
- ⚠ R 해금 레벨은 `[자체]` 로 표기하고, 원작 값이 확인되면 애셋만 바꾼다
